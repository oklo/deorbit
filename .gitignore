# local daemon state / checkpoints
state/
*.pid
*.log

# gated third-party code (iSALE-Dellen) — do NOT redistribute
iSALE2D/

# python
__pycache__/
*.pyc
.venv/
.DS_Store

# high-fidelity ephemeris binaries (downloaded, large)
highfidelity/data/

# aerobraking run output (reproducible)
highfidelity/state_aerobrake/

# survival-map sweep output (reproducible)
highfidelity/state_sweep/

# C++ SPH build artifacts
sph/sod
sph/sod_out.txt
*.o
sph/eos_test
sph/sod_run.log
sph/strength_test
sph/pi_scaling
sph/pi_sweep.log
sph/cayambe
sph/cayambe_snap*.csv
sph/cayambe_run.log
sph/run_ic
sph/run_ic2
sph/cayambe_ic*.bin
sph/ic_snap*.csv
sph/dem/
sph/proto_*.png
sph/offscreen_test.png
sph/recon_*.png
sph/ic_snap*.bin
sph/edifice_*.png
sph/edifice_preview.log
sph/frames/
sph/*.gif
sph/latest_*.png
sph/preimpact_*.png
sph/hero_*.png
sph/*.mp4
gpu/metal-cpp/
gpu/*.air
gpu/*.metallib
gpu/hello
gpu/density_test
sph/iron*.png
gpu/*.bin
gpu/*.log
gpu/cells_test
gpu/eos_gpu_test
gpu/force_test
gpu/step_test
gpu/gpu_run
gpu/gpu_ic
gpu/cpu_dump
gpu/cpu_bench
gpu/frames/
gpu/*.png

# snapshot / checkpoint outputs (large, regenerable — never commit).
# GPU (gpu/) is the production path now; CPU (sph/) is the validation oracle.
# GPU snapshots (gpu/gpu_snap_*.bin) are already covered by gpu/*.bin above.
sph/frac_snap*.bin
sph/frac_snap*.csv
sph/frac_chk.bin
sph/frac_*.png
sph/ground_test.png
# rendered run movies (regenerable from gpu_snap_* / frames/)
gpu/*.mp4
gpu/*.gif

# Orcus: MOLA tiles (downloadable) + large 1km ICs (regenerable)
mars_data/
sph/orcus*.bin
